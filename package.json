{
  "name": "nuxt-app",
  "private": true,
  "homepage": "https://Stasies.github.io/denis",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "predeploy" : "npm run build",
"deploy" : "gh-pages -d build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "gh-pages": "^5.0.0",
    "node-sass": "^8.0.0",
    "nuxt": "^3.4.2"
  },
  "dependencies": {
    "@nuxt/types": "^2.16.3",
    "sass": "^1.56.1"
  }
}
